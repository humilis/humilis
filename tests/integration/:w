"""Create and destroy the test environments for various humilis plugins."""

import os
import pytest


EXAMPLES_DIR = os.path.abspath(
    os.path.join(os.path.dirname(__file__), "..", "..", "examples"))

EXAMPLE_ENVS = [
    os.path.join(EXAMPLES_DIR, env) for env in os.listdir(EXAMPLES_DIR)
    if not os.path.isdir(os.path_join(EXAMPLES_DIR, env))]



def test_plugin():
    """Create and destroy the test environment of a humilis plugin."""
    import pdb; pdb.set_trace()
    pass



